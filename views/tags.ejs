<!DOCTYPE html>
<html>
  <head>
    <title>Tags</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <p>The existent tags are:</p>
    <% tags.forEach(function(tag) { %>
      <p><%= tag %></p>
    <% }); %>
  </body>
</html>
